var favouriteMeal = "BREAKFAST";

console.log(favouriteMeal.toLowerCase());

// const rolledDice = function (num) {
//   results = "";
//   for (let i = 0; i < num; i++) {
//     if (i === num - 1) {
//       results += Math.floor(Math.random() * 6) + 1;
//     } else {
//       results += `${Math.floor(Math.random() * 6) + 1}, `;
//     }
//   }
//   return results;
// };

// const numDice = 3;
// console.log(`Rolled ${numDice} dice: ${rolledDice(numDice)}`);
